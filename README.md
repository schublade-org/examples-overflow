# Overflow

Avatar group at the extreme (12 total, 2 visible), plus chip, button, and accordion copy that does not fit a tidy label.

```bash
npx schublade serve --config ./schublade.toml
# or ./run.sh
```

http://127.0.0.1:47306
